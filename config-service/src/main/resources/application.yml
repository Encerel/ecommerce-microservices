server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
#        git:
#          uri: https://github.com/Encerel/ecommerce-microservices-config.git
#          search-paths: product-service
#        vault:
#          port: 8200
#          host: 127.0.0.1
#          kv-version: 2
